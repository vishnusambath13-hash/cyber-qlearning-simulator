# ============================================================
# REQUIREMENTS.TXT
# Python dependencies for the Cyber Battle Q-Learning backend.
# Install with: pip install -r requirements.txt
# Tested with Python 3.10+
# ============================================================

# --- Web framework ---
fastapi==0.111.0

# --- ASGI server (runs the FastAPI app) ---
uvicorn==0.29.0

# --- Data validation & serialization (FastAPI dependency) ---
pydantic==2.7.1

# --- Async SQLite driver ---
aiosqlite==0.20.0

# --- CORS + static file serving ---
# Both are included with fastapi[all], listed explicitly for clarity
python-multipart==0.0.9

# --- Standard library used (no install needed, listed for clarity) ---
# sqlite3  — sync SQLite, used for initial DB setup
# json     — serializing Q-tables to TEXT columns
# math     — softmax, random
# random   — epsilon-greedy exploration
# os       — path resolution for database file
# typing   — type hints throughout